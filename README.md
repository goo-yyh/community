# 未来小区综合服务小程序

基于既有后台管理内容，改造成 Vue + Vite + Vant 的移动端小程序界面。

## 本地运行

```bash
npm install
npm run dev
```

## 验证

```bash
npm run lint
npm run build
```

## Vercel

项目已包含 `vercel.json`，Vercel 使用 `npm ci` 安装依赖、`npm run build` 构建，输出目录为 `dist`。
